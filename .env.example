# Copy this file to .env and fill in real values. .env is gitignored — never commit it.

# SMB (router-USB / NAS) share used for Kodi backup storage.
SMB_HOST=192.168.1.1
SMB_SHARE=Kodi
SMB_USER=changeme
SMB_PASS=changeme
SMB_BACKUP_DIR=backups
